
# Dolt database files (added by bd init)
.dolt/
*.db
*.db-shm
*.db-wal
*.db-journal

# Beads per-repo credential encryption key (secret - never commit)
.beads/.beads-credential-key

# Claude Code agent workspace
.claude/

# Local secrets — never commit (real key lives here; .env.example is the template)
.env

# Prototype app build/runtime artifacts
node_modules/
apps/notes/vault/
apps/notes/vault-demo/
apps/todo/todos.md
# Generated OpenAPI specs (regenerated by start.sh from each app's live server)
apps/nutrition/openapi.json
apps/notes/openapi.json
apps/todo/openapi.json
