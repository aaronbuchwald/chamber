# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# SQLite / Dolt databases
.dolt/
*.db
*.db-shm
*.db-wal
*.db-journal

# Beads per-repo credential encryption key (secret — never commit)
.beads/.beads-credential-key

# Local secrets — never commit (real key lives here; .env.example is the template)
.env

# Claude Code agent workspace
.claude/

# OS / editor noise
.DS_Store

# NOTE: generated proto code (**/gen) is intentionally COMMITTED and verified in CI.
