{
  "name": "@chamber/console",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "A generic, dependency-free HTML console for any @chamber/datagram app. Renders a form-per-operation from operation metadata and runs them against the live HTTP routes. A UI component — it imports only the public AppDef/Operation types of @chamber/datagram, never its runtime.",
  "license": "MIT",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --import tsx --test test/*.test.ts"
  },
  "dependencies": {
    "@chamber/datagram": "*"
  }
}
